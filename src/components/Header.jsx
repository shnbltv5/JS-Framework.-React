export default function Header() {
  return (
    <header className="site-header">
      <span className="logo">A.S.</span>
      <nav>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
      </nav>
    </header>
  );
}
